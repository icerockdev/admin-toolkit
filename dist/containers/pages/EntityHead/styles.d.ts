import { Theme } from '@material-ui/core';
declare const _default: (theme: Theme) => {
    header: {
        marginBottom: number;
    };
    title: {
        color: string;
        fontSize: string;
        fontWeight: number;
        flexGrow: number;
    };
};
export default _default;
