import { Theme } from '@material-ui/core';
declare const _default: (theme: Theme) => {
    breadcrumbs: {
        alignItems: string;
        display: string;
        padding: string;
    };
};
export default _default;
