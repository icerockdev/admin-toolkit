import { Theme } from '@material-ui/core';

export default (theme: Theme) => ({
  breadcrumbs: {
    marginBottom: theme.spacing(2),
    height: 36,
    alignItems: 'center',
    display: 'flex',
  },
});
